global:
  scrape_interval: 5s

scrape_configs:
  - job_name: "fraud-monitoring"
    metrics_path: /metrics
    static_configs:
      - targets: ["monitoring:9090"]

  - job_name: "fraud-predict-service"
    metrics_path: /metrics
    static_configs:
      - targets: ["predict-service:8000"]
